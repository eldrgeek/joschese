# Task: build the Joscha Bach thinker-site (ingest + scaffold + local build ONLY)

You are an engineering worker on Mike's Mac. Full host access. Work in `~/Projects`.
Read `~/Projects/Joscha/BUILD-JOSCHA.md` first — it is your spec. Then execute it.

## HARD CONSTRAINTS (do not violate)
- **NO deploy. NO publishing.** Do NOT run `netlify` (any subcommand), do NOT
  `git push`, do NOT create any GitHub repo or Netlify site, do NOT link a remote.
- Local git `init`/`add`/`commit` inside `~/Projects/Joscha` is fine. Pushing is NOT.
- Do NOT touch any directory other than `~/Projects/Joscha` (you may READ
  `~/Projects/Levinese` and `~/Projects/SOMA` and `~/Projects/FrontRow` as templates/source).
- Do NOT print or commit the contents of `~/Projects/Joscha/.env`.
- Stop when `npm run build` passes in `~/Projects/Joscha`. Then write a report.

## Steps
1. **Ingest** Joscha's X content (the venv + key are already set up):
   `cd ~/Projects/Joscha && ingest/.venv/bin/python ingest/pull_joscha_x.py`
   This writes `ingest/out/xposts/*.json` and `ingest/out/terms_candidates.json`.
   If it errors on rate limits, reduce scope (e.g. fewer TOPICS or wider date
   windows) and continue — partial content is acceptable for v0.
2. **Scaffold** the Astro site from the `~/Projects/Levinese` template per
   BUILD-JOSCHA.md Step 2 (copy structure, NOT content; Joscha-ize branding;
   xposts+videos+terms+atlas as the spine; de-emphasize `papers`). Working
   wordmark: **"Joschese — a field guide to Joscha Bach's ideas"**.
3. Drop `ingest/out/xposts/*.json` into `src/content/xposts/`. Seed
   `src/content/terms/*.md` from `ingest/out/terms_candidates.json` matching the
   Levinese `terms` frontmatter schema (authored_by: "Joscha Bach").
4. **Guide**: create `public/joscha-guide-config.js` per BUILD-JOSCHA.md Step 3 —
   an AI guide GROUNDED IN Joscha's corpus, NOT impersonating him. Reuse a
   `joscha` SOMA Campus ConvAI agent if one exists; otherwise leave
   `voiceAgentId` null with a TODO and ship text-only. Copy the SomaAuth head
   block + `public/js/soma-auth*.js` from the updated Levinese `Base.astro`.
5. `npm install && npm run build`. Fix all errors until the build passes.
6. Write `~/Projects/Joscha/BUILD-REPORT.md`: what you built, file counts
   (xposts, terms), build result, the Netlify site name you SUGGEST (do not
   create it), and anything left for Mike.
